# Per-device data that other modules read. Keep this minimal — feature
# flags belong inline in default.nix as workstation.<feature>.enable.
{
  gitUsername = "mavnezz";
  gitEmail = "sirjuls44@github.com";

  # Niri output scale — surface is HiDPI 1.5x
  niriOutputScale = 1.5;
}
